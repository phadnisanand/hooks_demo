<?php
/**
 * @file
 * Contains servicedemo.module.
 */

/**
 * Implements hook_theme().
 */
function servicedemo_theme($existing, $type, $theme, $path) {
  return array(
    'service_event' => array(
      'variables' => array(
        'events' => [],
      ),
    ),
  );
}
